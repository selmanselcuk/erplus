name: erplus
description: "ERPlus – Flutter ERP uygulaması."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # iOS tarzı ikonlar
  cupertino_icons: ^1.0.8

  # UI – opsiyonel, ileride kullanırsın (şimdilik yorum satırı)
  # provider: ^6.1.2
  # http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Flutter lints – Flutter 3.22 ile UYUMLU sürüm
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Varlık eklemek istersen burayı aç
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Özel font eklemek istersen:
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700
